<?xml version="1.0" encoding="utf-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-4.2.xsd"
       default-autowire="byName">


    <bean id="ds_lc" class="com.alibaba.druid.pool.DruidDataSource"
          destroy-method="close" depends-on="propertyConfigurer">
        <property name="driverClassName" value="${driver}" />
        <property name="url" value="${url.lc}" />
        <property name="username" value="${username.lc}" />
        <property name="password" value="${password.lc}" />

    </bean>

    <bean id="ds_lc_0" class="com.alibaba.druid.pool.DruidDataSource"
          destroy-method="close" depends-on="propertyConfigurer">
        <property name="driverClassName" value="${driver}" />
        <property name="url" value="${url.lc_0}" />
        <property name="username" value="${username.lc}" />
        <property name="password" value="${password.lc}" />
    </bean>


    <bean id="ds_lc_1" class="com.alibaba.druid.pool.DruidDataSource"
          destroy-method="close" depends-on="propertyConfigurer">
        <property name="driverClassName" value="${driver}" />
        <property name="url" value="${url.lc_1}" />
        <property name="username" value="${username.lc}" />
        <property name="password" value="${password.lc}" />
    </bean>

    <bean id="ds_lc_2" class="com.alibaba.druid.pool.DruidDataSource"
          destroy-method="close" depends-on="propertyConfigurer">
        <property name="driverClassName" value="${driver}" />
        <property name="url" value="${url.lc_2}" />
        <property name="username" value="${username.lc}" />
        <property name="password" value="${password.lc}" />
    </bean>

    <bean id="ds_lc_3" class="com.alibaba.druid.pool.DruidDataSource"
          destroy-method="close" depends-on="propertyConfigurer">
        <property name="driverClassName" value="${driver}" />
        <property name="url" value="${url.lc_3}" />
        <property name="username" value="${username.lc}" />
        <property name="password" value="${password.lc}" />
    </bean>

    <bean id="ds_lc_4" class="com.alibaba.druid.pool.DruidDataSource"
          destroy-method="close" depends-on="propertyConfigurer">
        <property name="driverClassName" value="${driver}" />
        <property name="url" value="${url.lc_4}" />
        <property name="username" value="${username.lc}" />
        <property name="password" value="${password.lc}" />
    </bean>

    <bean id="ds_lc_5" class="com.alibaba.druid.pool.DruidDataSource"
          destroy-method="close" depends-on="propertyConfigurer">
        <property name="driverClassName" value="${driver}" />
        <property name="url" value="${url.lc_5}" />
        <property name="username" value="${username.lc}" />
        <property name="password" value="${password.lc}" />
    </bean>

    <bean id="ds_lc_6" class="com.alibaba.druid.pool.DruidDataSource"
          destroy-method="close" depends-on="propertyConfigurer">
        <property name="driverClassName" value="${driver}" />
        <property name="url" value="${url.lc_6}" />
        <property name="username" value="${username.lc}" />
        <property name="password" value="${password.lc}" />
    </bean>

    <bean id="ds_lc_7" class="com.alibaba.druid.pool.DruidDataSource"
          destroy-method="close" depends-on="propertyConfigurer">
        <property name="driverClassName" value="${driver}" />
        <property name="url" value="${url.lc_7}" />
        <property name="username" value="${username.lc}" />
        <property name="password" value="${password.lc}" />
    </bean>

    <bean id="ds_lc_8" class="com.alibaba.druid.pool.DruidDataSource"
          destroy-method="close" depends-on="propertyConfigurer">
        <property name="driverClassName" value="${driver}" />
        <property name="url" value="${url.lc_8}" />
        <property name="username" value="${username.lc}" />
        <property name="password" value="${password.lc}" />
    </bean>

    <bean id="ds_lc_9" class="com.alibaba.druid.pool.DruidDataSource"
          destroy-method="close" depends-on="propertyConfigurer">
        <property name="driverClassName" value="${driver}" />
        <property name="url" value="${url.lc_9}" />
        <property name="username" value="${username.lc}" />
        <property name="password" value="${password.lc}" />
    </bean>


</beans>

