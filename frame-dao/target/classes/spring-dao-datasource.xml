<?xml version="1.0" encoding="utf-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-4.2.xsd"
       default-autowire="byName">


    <bean id="ds_lc" class="com.alibaba.druid.pool.DruidDataSource"
          destroy-method="close" depends-on="propertyConfigurer">
        <property name="driverClassName" value="${driver}" />
        <property name="url" value="${url.lc}" />
        <property name="username" value="${username.lc}" />
        <property name="password" value="${password.lc}" />

    </bean>

    <bean id="ds_lc_00" class="com.alibaba.druid.pool.DruidDataSource"
          destroy-method="close" depends-on="propertyConfigurer">
        <property name="driverClassName" value="${driver}" />
        <property name="url" value="${url.lc_0}" />
        <property name="username" value="${username.lc}" />
        <property name="password" value="${password.lc}" />
    </bean>


    <bean id="ds_lc_01" class="com.alibaba.druid.pool.DruidDataSource"
          destroy-method="close" depends-on="propertyConfigurer">
        <property name="driverClassName" value="${driver}" />
        <property name="url" value="${url.lc_1}" />
        <property name="username" value="${username.lc}" />
        <property name="password" value="${password.lc}" />
    </bean>

    <bean id="ds_lc_02" class="com.alibaba.druid.pool.DruidDataSource"
          destroy-method="close" depends-on="propertyConfigurer">
        <property name="driverClassName" value="${driver}" />
        <property name="url" value="${url.lc_2}" />
        <property name="username" value="${username.lc}" />
        <property name="password" value="${password.lc}" />
    </bean>

    <bean id="ds_lc_03" class="com.alibaba.druid.pool.DruidDataSource"
          destroy-method="close" depends-on="propertyConfigurer">
        <property name="driverClassName" value="${driver}" />
        <property name="url" value="${url.lc_3}" />
        <property name="username" value="${username.lc}" />
        <property name="password" value="${password.lc}" />
    </bean>

    <bean id="ds_lc_04" class="com.alibaba.druid.pool.DruidDataSource"
          destroy-method="close" depends-on="propertyConfigurer">
        <property name="driverClassName" value="${driver}" />
        <property name="url" value="${url.lc_4}" />
        <property name="username" value="${username.lc}" />
        <property name="password" value="${password.lc}" />
    </bean>

    <bean id="ds_lc_05" class="com.alibaba.druid.pool.DruidDataSource"
          destroy-method="close" depends-on="propertyConfigurer">
        <property name="driverClassName" value="${driver}" />
        <property name="url" value="${url.lc_5}" />
        <property name="username" value="${username.lc}" />
        <property name="password" value="${password.lc}" />
    </bean>

    <bean id="ds_lc_06" class="com.alibaba.druid.pool.DruidDataSource"
          destroy-method="close" depends-on="propertyConfigurer">
        <property name="driverClassName" value="${driver}" />
        <property name="url" value="${url.lc_6}" />
        <property name="username" value="${username.lc}" />
        <property name="password" value="${password.lc}" />
    </bean>

    <bean id="ds_lc_07" class="com.alibaba.druid.pool.DruidDataSource"
          destroy-method="close" depends-on="propertyConfigurer">
        <property name="driverClassName" value="${driver}" />
        <property name="url" value="${url.lc_7}" />
        <property name="username" value="${username.lc}" />
        <property name="password" value="${password.lc}" />
    </bean>

    <bean id="ds_lc_08" class="com.alibaba.druid.pool.DruidDataSource"
          destroy-method="close" depends-on="propertyConfigurer">
        <property name="driverClassName" value="${driver}" />
        <property name="url" value="${url.lc_8}" />
        <property name="username" value="${username.lc}" />
        <property name="password" value="${password.lc}" />
    </bean>

    <bean id="ds_lc_09" class="com.alibaba.druid.pool.DruidDataSource"
          destroy-method="close" depends-on="propertyConfigurer">
        <property name="driverClassName" value="${driver}" />
        <property name="url" value="${url.lc_9}" />
        <property name="username" value="${username.lc}" />
        <property name="password" value="${password.lc}" />
    </bean>


</beans>

